

<div>
        status = {{ status }}
</div>
{#
<br>


<div style="overflow-x:auto;">
    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">
                <div class="d-grid">
                        {% if desk1 is defined %}
                                {% if desk1 == -1 %}
                                        <button class="btn btn-primary" type="button" onClick = "sortTable({{ status }}, 1, 1)">id
                                                <img src="static/images/sortl.png" alt="Сортировка"></button>
                                {% else %}
                                        <button class="btn btn-primary" type="button" onClick = "sortTable({{ status }}1, -1)">id
                                                <img src="static/images/sortb.png" alt="Сортировка"></button>
                                {% endif %}
                        {% else %}
                                <button class="btn btn-primary" type="button" onClick = "sortTable( {{ status }}, 1, -1)">id</button>
                        {% endif %}
                </div>
            </th>
            <th scope="col">
                <div class="d-grid">
                        {% if desk2 is defined %}
                                {% if desk2 == -1 %}
                                        <button class="btn btn-primary" type="button" onClick = "sortTable({{ status }}, 2, 1)">created_at
                                                <img src="static/images/sortl.png" alt="Сортировка"></button>
                                {% else %}
                                        <button class="btn btn-primary" type="button" onClick = "sortTable({{ status }}2, -1)">created_at
                                                <img src="static/images/sortb.png" alt="Сортировка"></button>
                                {% endif %}
                        {% else %}
                                <button class="btn btn-primary" type="button" onClick = "sortTable( {{ status }}, 2, -1)">created_at</button>
                        {% endif %}
                </div>
            </th>
            <th scope="col">email</th>
            <th scope="col">roles</th>
            <th scope="col">enabled</th>
            <th scope="col"></th>
        </tr>
        </thead>
        <tbody>

        {% for user in users %}
            <tr>
                <th scope="row">{{ loop.index }}</th>
                <td>{{ user.id }}</td>
                <td>{{ user.created_at }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles }}</td>
                <td>{{ user.enabled }}</td>
                <td><a class="btn btn-primary" href="{{ path('user_enable', {'id': user.id}) }}" role="button">Подтвердить</a></td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
</div>
#}