{% extends 'admin/base_adm.html.twig' %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block content %}
    {% if is_granted('ROLE_ADMIN') %}
    <style>
        .uf_label {
            color: red;
        }
    </style>

    <h5 style="text-align: center">Пользователи</h5>
    <br>

        {{ form_start(form, {method: 'POST', action: path('users_post')}) }}
        <div class="row g-3">
            <div class="col-sm-3">
                {{ form_label(form._state, 'Статус: ', {'label_attr': {'class': 'uf_label'}} ) }}
                {{ form_widget(form._state) }}
            </div>

            <div class="col-1" style="align-self: flex-end">
                <button type="submit">Обновить</button>
            </div>
        </div>
        {{ form_end(form) }}


{#
    Так - в контроллере не будет $form->isSubmitted()

    <form class="row g-3" method="post" action="{{ path('users_post') }}">
        <div class="col md-3">
            <div class="input-group">
                <label class="input-group-text" for="status">Status</label>
                <select class="form-select" id="status"  name="st">
                    <option value="1">Все</option>
                    <option value="2">Подтверждённые</option>
                    <option value="3">Неподтверждённые</option>
                </select>
            </div>
        </div>
        <div class="col md-2">
            <button type="submit" class="btn btn-primary">Обновить</button>
        </div>
    </form>
#}

        <br>
{#
    <div style="overflow-x:auto;">
        <table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">
                    <div class="d-grid">
                        <button class="btn btn-primary" type="button" onClick = "sortTable(0, -1)">id</button>
                    </div>
                </th>
                <th scope="col">
                    <div class="d-grid">
                        <button class="btn btn-primary" type="button" onClick = "sortTable(0, -1)">created_at</button>
                    </div>
                </th>
                <th scope="col">email</th>
                <th scope="col">roles</th>
                <th scope="col">enabled</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>

                {% for user in users %}
                <tr>
                    <th scope="row">{{ loop.index }}</th>
                    <td>{{ user.id }}</td>
                    <td>{{ user.created_at }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.roles }}</td>
                    <td>{{ user.enabled }}</td>
                    <td><a class="btn btn-primary" href="{{ path('user_enable', {'id': user.id}) }}" role="button">Подтвердить</a></td>
                </tr>
            {% endfor %}

            </tbody>
        </table>
    </div>
#}

        <div id="table">
            Блок с таблицей, first.
            <br>
            <button class="btn btn-primary" type="button" onClick = "sortTable( {{ status }}, 0, -1)">id</button>
            <br>
            status = {{ status }}

        </div>

        <script>
            function sortTable( status, sortby, desc){
                $.ajax({
                    url: '{{ path('users_ajax') }}',
                    method: 'post',
                    dataType: 'html',
                    data: {status: status, sortby: sortby, desc:desc},
                    success: function (data) {
                        $("#table").html(data);
                    },
                });
            }
        </script>

    {% endif %}
{% endblock %}

